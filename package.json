{
  "name": "netiq",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix web && node scripts/sync-vercel-next-output.js",
    "dev": "npm run dev --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
